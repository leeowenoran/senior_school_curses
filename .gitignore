# WorkBuddy internal
.workbuddy/

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if added later)
node_modules/

# Local backup copies (large safety copies, not source)
_local_backup_*/

# Seed/preview templates (dev artifacts)
_seed*.html

# Untracked helpers not for production
1necho
server_nocache.py
cloudbaserc.json

# Dev-only folders (not served on CloudBase)
backend/
docs/
cloud_functions/
scripts/

# History deployment snapshots (workspace-only, never served)
gz_site_v*/
